bundle:
  name: Databricks-Analytics-Pipeline

variables:
  node_type_id:
    default: Standard_D4as_v5

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
      host: ${DATABRICKS_HOST}

  prod:
    mode: production
    workspace:
      host: ${DATABRICKS_HOST}      
        root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}