bundle:
  name: Dataaabricks-Analytics-Pipeline

workspace:
  # Keep it simple and stable
  root_path: /Shared/ingestion-pipeline/${bundle.target}

include:
  - resources/*.yml

targets:
  dev:
    mode: development

  prod:
    mode: production