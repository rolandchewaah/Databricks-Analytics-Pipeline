bundle:
  name: Databricks-Analytics-Pipeline

workspace:
  # Keep it simple and stable
  root_path: /Shared/Databricks-Analytics-Pipeline/${bundle.target}

include:
  - resources/*.yml

targets:
  dev:
    mode: development

  prod:
    mode: production